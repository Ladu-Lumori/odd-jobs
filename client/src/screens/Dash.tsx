import * as React from 'react';
import { Button,
  Stack,
  Grid,
  Box,
  SwipeableDrawer,
  List,
  Divider,
  ListItem,
  ListItemButton,
  ListItemIcon,
  ListItemText,
  Tab,
  Tabs,
  Typography
} from '@mui/material';

export default function Dash(){
  return(
    <Typography variant="h1" gutterBottom>
        Your Dashboard.
    </Typography>
  );
}
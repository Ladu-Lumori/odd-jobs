import * as React from 'react';
import { Button,
  Stack,
  Grid,
  Box,
  SwipeableDrawer,
  List,
  Divider,
  ListItem,
  ListItemButton,
  ListItemIcon,
  ListItemText,
  Tab,
  Tabs,
  Typography
} from '@mui/material';

export default function Settings(){
  return(
    <Typography variant="h2" gutterBottom>
        Settings.
    </Typography>
  );
}